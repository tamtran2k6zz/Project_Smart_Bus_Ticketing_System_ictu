{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
   "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "build": "tsc",
    "start": "node dist/main.ts",
    "lint": "eslint \"src/**/*.{js,ts}\"",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  }
}
